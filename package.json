{
  "name": "team-task-manager",
  "version": "1.0.0",
  "description": "Full-Stack Team Task Manager",
  "main": "server/index.js",
  "scripts": {
    "start": "npx prisma generate --schema=./server/prisma/schema.prisma && npx prisma db push --schema=./server/prisma/schema.prisma && node server/index.js",
    "build": "npm install --prefix client && npm run build --prefix client && npx prisma generate --schema=./server/prisma/schema.prisma"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "jsonwebtoken": "^9.0.2",
    "bcryptjs": "^2.4.3",
    "@prisma/client": "^5.7.1",
    "prisma": "^5.7.1"
  }
}
